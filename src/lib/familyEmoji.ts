// A family's emoji is optional: owners pick one in settings, and until they do
// the app falls back to the shopping cart so every switcher row still reads as
// one tidy tile. The settings picker shows the same fallback (dimmed) so the
// preview never sits empty.
export const DEFAULT_FAMILY_EMOJI = '🛒'

// Curated household/family emoji offered in the settings picker.
export const FAMILY_EMOJIS = [
  '🏠', '🏡', '🛒', '🧺', '🍎', '🥕', '🍞', '🥑', '🍕',
  '🍽️', '☕', '🐶', '🐱', '🌿', '🌟', '❤️', '🎉', '🔑',
]
